# 独立发布包验证

日期：2026.09.23；Windows，Python 3.13.5。

- 在导出的`app/`目录执行四个既有检查模块：46项通过，用时12.004秒。真实传输由替身替代，模型调用为0。
- 导出后的`app.js`和`chat.js`均通过Node语法检查。
- 将发布代码复制到独立临时目录，默认mock成功启动，并自动创建新空库。首页、两份JavaScript、CSS、字体及bootstrap共6个HTTP资源返回成功，模式确认为mock。
- 补充版本与路线图后，两包合计20处文档链接、245处HTML资源／锚点引用检查通过；无本机绝对路径、已列明的排除材料或密钥格式命中。该扫描只检查选定格式，不等于完整安全审计。

本包只带代码、文档与模拟界面截图，不带原始数据库或运行日志。自动化结果不代表真实模型口吻、生产账号隔离或用户效果通过。
